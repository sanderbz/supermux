[blocker] The chat WS route cannot live in `http::protected_router` as §2.1 states — that router applies a bearer middleware a browser WebSocket cannot satisfy.
- Evidence: `server/src/http.rs:166` `.layer(from_fn_with_state(state, auth::auth_middleware))`; `server/src/auth.rs:27-33` rejects any request without `Authorization: Bearer` (or `?_token=`, which would leak the dashboard bearer into URLs/logs). This is precisely why `ws::router_for` is merged separately at `server/src/http.rs:46`, outside the auth layer, with in-band first-frame auth (`server/src/ws/mod.rs:155-165`).
- Fix: split the module — REST bits (`/api/sessions/{name}/chat/seed`) into `protected_router`; register `GET /ws/sessions/{name}/chat` inside `ws::router_for` and reuse `origin_allowed` + `verify_auth_frame` + the 2s `AUTH_TIMEOUT`. Do not use `?_token`.

[blocker] Subagent transcripts are not sidechains in the main JSONL on the deployed Claude Code — the parser design in §2.1/§4.2 targets a shape that does not exist.
- Evidence: live corpus `~/.claude/projects/-opt-projects-supermux/61577886-e091-4429-a739-412886e6752d.jsonl` — 3397 lines, `isSidechain:true` count = **0**. Subagent transcripts are separate files: `…/61577886-…/subagents/agent-<id>.jsonl` (`isSidechain:true`, `agentId`, `promptId`) plus `agent-<id>.meta.json` = `{"agentType","description","toolUseId","spawnDepth","model"}`. `recall.rs`'s `include_sidechains` flag is therefore also dead for this version.
- Fix: tail `<conv-id>/subagents/*.jsonl` as a second watch scope, key each agent by `agentId`, and join to the parent's Task `tool_use` via `meta.json.toolUseId`. Lazy-fetch the agent file on card expand; do not try to reconstruct from the main file.

[blocker] The "`UseLiveTermResult`-compatible subset" claim breaks at the real call sites — `send()` there is a raw pty write with an explicit CR, which REST `/send` cannot emulate.
- Evidence: `web/src/components/focus-mode/desktop-split.tsx:654` `onRunSlash={(cmd) => termRef.current?.send(cmd + '\r')}`, `:685` `onRun={(body) => …send(body + '\r')}`, `:684` `onInsert={(body) => …send(body)}` (insert **without** submit); `web/src/routes/focus/mobile.tsx:601` same. `lifecycle::send_text` (`server/src/sessions/lifecycle.rs:1126-1160`) already appends the submit gap + Enter → `send(x + '\r')` double-submits, and `/send` has no insert-only mode. Also `tryOpenLinkAt` is used at `mobile.tsx:254`, and `UseLiveTermResult` (`web/src/hooks/use-live-term.ts:43-110`) is a non-optional interface — a "subset" won't typecheck against these props.
- Fix: don't shim the terminal handle. Define a narrow `SessionInput` interface (`submit(text)`, `insert(text)`, `sendKey(name)`, `focus/blur`) implemented by both renderers, and change the three parents' prop types. Map `insert` to `POST /paste {submit:false}` (`server/src/sessions/mod.rs:1139-1152`), `submit` to `/send`. Keep `tryOpenLinkAt` out of the shared interface (chat DOM has native links).

[major] The perf-budget claim "budgets hold" is already 77% consumed, and making chat the default moves the markdown vendor chunk onto the hero path.
- Evidence: `node web/scripts/size-budget.mjs` on checked-in dist → **app JS total 154.34 KB / 200 KB (77%)**; `vendor-markdown-prZNuY3Y.js` = 92.83 KB gz, currently only reached via the lazy `markdown-viewer` chunk. `web/perf/baselines/baseline.json` still records 44.8 KB (measured 2026-05-22) — the plan is citing a stale headroom figure. Budget excludes `vendor-*` (`size-budget.mjs:57`), so a 92.83 KB regression on first paint is invisible to the gate.
- Fix: make fase 2 gate on *both* app-JS total and a new "hero-path vendor" number; keep the chat renderer + markdown/rehype pipeline in a lazy chunk with a terminal/skeleton shell paint first; refresh `baseline.json` before writing any budget assertion into a plan.

[major] "Overview chat-tail = cheap REST per tile, no WS" is contradicted by how transcript reads actually work, and reinvents a mechanism that already exists.
- Evidence: `server/src/sessions/recall.rs:392 read_user_turns` opens the file and walks it **fully forward**, then reverses — no tail seek. The live transcript here is 8.9 MB / 3397 lines with a max single line of **950,712 bytes**. 20 Claude tiles × a full-file scan per SSE tick is a `spawn_blocking` pool killer. Meanwhile a per-session tail already rides the existing SSE `sessions` delta for free: `server/src/sessions/auto_actions.rs:757` and `:1057 tail_lines` → `SessionView::preview_lines` (`sessions/mod.rs:168,266`).
- Fix: the tailer keeps the last N parsed entries per session in memory (it is already reading the file incrementally); publish a `chat_tail` field on the *same* `sessions` SSE delta. Zero per-tile requests, zero new endpoint. Close open question §9.2 that way now.

[major] The approval card cannot be populated from the hook — payload is truncated at 16 KB and `ToolInput` deliberately discards everything the card needs.
- Evidence: `server/src/claude_config.rs:396` `D=$(head -c 16384)`; `server/src/sessions/activity.rs:61-73` `ToolInput` deserializes only `command/description/file_path/pattern/url` and the doc at `:59-60` states "Anything else (notably Edit/Write `content`) is ignored". A truncated slurp yields invalid JSON → `hooks.rs:123-126` lenient parse → `HookPayload::default()` → **empty card**, silently.
- Fix: source approval-card *content* from the transcript `tool_use` entry (full fidelity, already on disk) and use `PermissionRequest` only as the live *trigger*; or give that one event its own uncapped command + a dedicated wider payload struct.

[major] Adding `PermissionRequest` via the shared `EVENTS` loop writes a decision hook whose stdout is `{"ok":true}` — into the *global* settings file.
- Evidence: `server/src/claude_config.rs:54-68` EVENTS → `supermux_entry` → `hook_command` (`:395-402`) uses `curl -fsS …` with no `-o /dev/null`, so the response body goes to stdout. Claude already ingests that stdout: the live transcript contains **832 `attachment{type:"hook_success"}` entries out of 3397 lines** (~25% of the file, and context). `PermissionRequest` is decision-capable (report-claude-wrapping.md:314) and `PostToolUseFailure` exists in the deployed binary (`grep -a` on `~/.local/share/claude/versions/2.1.227`: 76 and 29 hits; installed version confirmed `claude --version` → 2.1.227, so the version gate passes). Install target is `~/.claude/settings.json` (`claude_config.rs:126,136`) — every Claude on the box, including ones with no `$SUPERMUX_SESSION`.
- Fix: add `-o /dev/null` to `hook_command` (fixes the existing 25%-of-transcript noise too), and make `supermux_entry` per-event so decision hooks never emit stdout. Note `PostToolUseFailure` is already handled server-side at `hooks.rs:173-176` — only the EVENTS line is missing; and drop the now-false comment there ("Claude has no dedicated PostToolUseFailure event").

[major] The Attention card's "live mini `capture_screen_ansi` snapshot via existing `/peek`, rendered through `lib/ansi.ts`" does not work as described.
- Evidence: `server/src/sessions/lifecycle.rs:1222` — `peek` returns `rt.capture_plain(lines)`, **not** ANSI. `web/src/lib/ansi.ts:6-8` states it is SGR-only and "Cursor moves and other CSI sequences are dropped (the preview is a frozen tail, not a live screen)" — i.e. exactly wrong for a box-drawn, cursor-positioned TUI dialog, which is the only thing the Attention card exists to show.
- Fix: `capture_ansi` already exists on the runtime trait (`server/src/sessions/runtime.rs:121`) — add `?ansi=1` to `peek_handler`; but render it in a read-only `LiveTerminal` (xterm) rather than `ansi.ts`, or just make the Attention card a one-tap toggle with no preview. A half-parsed dialog screenshot is worse than none.

[major] Nothing sets pty geometry when no terminal client ever attaches — "pinned by the terminal renderer / last-known geometry" has no source in a chat-default world.
- Evidence: `rt.resize` is called from exactly two places, both the terminal WS: `server/src/ws/mod.rs:851` and `:1336`. Nothing in `lifecycle.rs` resizes. Native pty boots at `(80, 24)` (`server/src/sessions/native/holder.rs:101`). A session created and only ever used in chat runs Claude at 80×24 forever; the first terminal toggle then fires the *first* resize → the re-emit/reflow class the plan is escaping, at the worst moment.
- Fix: set an explicit geometry at session create and on chat attach (one-shot resize to e.g. 120×40), persist last-known geometry on the session row, and make the chat renderer restore it on attach rather than "never resize".

[major] §2.3 lists "Permission prompt | `Notification` hook (`permission_prompt`)" — there is no such typed subtype; distinguishing it requires the English string matching the plan claims to have eliminated.
- Evidence: `server/src/sessions/activity.rs:48-50` — `Notification` carries only free-text `message`. `server/src/sessions/status.rs:380-385` documents that Claude Code "ALSO fires a `Notification` ~60s after a turn finishes ('Claude is waiting for your input')", which is why the state machine only treats it as decisive when it's the newest signal mid-turn. So `Waiting` alone can mean permission prompt, plan approval, trust dialog, or a 60-second idle nag.
- Fix: make `PermissionRequest` the *only* permission signal (it is typed), and specify that `Notification` contributes ambient Waiting only. Delete the `permission_prompt` label from §2.3 so fase 3 doesn't build a card on it.

[minor] Two transcript-shape assertions are wrong against the live corpus: compaction does **not** rotate the file, and "eight primitives" is not the complete vocabulary.
- Evidence: same corpus — a single `type:"system", subtype:"compact_boundary"` entry sits *inside* the file (line 18) with `logicalParentUuid`, and `SessionStart:compact` fired with an unchanged `sessionId`; only **one** `sessionId` appears across all 3397 lines. The plan §2.1 and the comment at `server/src/hooks.rs:133-134` both assert "compaction forks a fresh `<session_id>.jsonl`". Top-level types actually present: `assistant, attachment(952), user, system, last-prompt, custom-title, mode, permission-mode, pr-link, queue-operation(72), file-history-snapshot, file-history-delta` — `queue-operation` is the steering/queued-message surface fase 3 wants and has no primitive. Assistant entries are one content-block per line (`tool_use` 482 / `thinking` 403 / `text` 299, never mixed), which does support the block-level-flush claim.
- Fix: keep the byte cursor across compaction (no re-resolve needed); render `compact_boundary` as the system pill; add a 9th primitive for queued/steered messages; and cap per-entry payloads on the wire (recall already caps at 8000/600 chars, `recall.rs:75-79`) — with 950 KB single lines, an uncapped 50-entry seed is tens of MB.

[minor] The §6 applicability guard omits remote-host sessions, which have no local transcript to tail.
- Evidence: `server/src/db/sessions.rs:56` `pub host_id: Option<i64>`; hooks/settings are installed over `SshFileTransport` to the *remote* `~/.claude/settings.json` (`server/src/claude_config.rs:128-138`), and the JSONL lives on that host. §6 guards only on `provider === 'claude'` and team role, so a remote Claude session would offer chat and render permanently empty.
- Fix: add `host_id == null` to the guard (server-side too, so the WS 404s rather than silently tailing a nonexistent path), and list remote hosts in §7's "Later" alongside codex/kimi.
