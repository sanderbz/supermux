[blocker] The plan has no model for the user's own in-flight message — Claude Code queues prompts submitted mid-turn and nothing reaches the JSONL until the queue is consumed, so a chat send can vanish from the chat for minutes.
- Evidence: plan §2.3 line 59 sources "User prompt" *only* from the transcript `user` entry; §3 line 81 sends mid-turn via `POST /send` unconditionally; the transcript is written on consumption, not on submit (report-claude-wrapping.md:434-439 "no permission prompts or interactive state reflected", complete-messages-only). Fase 3 mentions "queued messages shown as pending pills" (plan:180) but only for supermux *steering* (`server/src/sessions/steering/`), which is a different queue with different semantics (delivered at turn boundary, single-flight).
- Why it matters: "my message appears the instant I hit Enter" is the one non-negotiable chat affordance. Without it, the flagship default renderer looks broken on the most common action.
- Fix: make the pending-message store a first-class part of §2/§4.2 — client-side optimistic entry keyed by `(last_send_text, last_send_at)`, rendered as a `queued` pill, reconciled/deduped when the matching transcript `user` entry arrives (match on normalized text + monotonic ts, with a TTL), plus an explicit decision on whether chat-composer sends mid-turn should route to `/steer` instead of `/send`. Add "does CC write queued prompts to JSONL at submit or at consumption?" to the fase-0 spike checklist.

[blocker] The outflow design assumes the TUI composer is empty; four existing code paths put text in it that the chat view cannot see, so a chat send can concatenate into a corrupted prompt or auto-submit a bare file path.
- Evidence: `web/src/components/focus-mode/use-attachment-upload.ts:1-17` — attachments inject the quoted absolute path "no prose, no trailing Enter … so the user can add their own wording and hit Enter themselves"; `web/src/components/focus-mode/dock.tsx:439-481` types slash-command *segments* (`onSend(seg + ' ')`) relying on the TUI's autocomplete; `web/src/components/snippets/snippet-panel.tsx:10,124` inserts a snippet body into the composer; and a user typing in the terminal view leaves a half-written line. Plan §4.1:95 makes `ChatRenderer` implement the `UseLiveTermResult` subset so "the dock/keybar/joystick parents keep working unmodified" — which is exactly what makes this fire. §3:87 dismisses this as "interleaves … acceptable, documented", which is only true for two *terminal* tabs where both users can see the composer.
- Fix: do not reuse those parents blindly. In chat mode, attachments/snippets/slash chips must insert into the React composer (client-side string), never the pty; and `send_text` from chat should first assert an empty composer (peek-verify, or an explicit "the terminal has an unsent draft" banner offering Open terminal). Spell out `send()` semantics in the shim: `/paste` (no submit) vs `/send` (submit) — callers today mean different things by the same method.

[blocker] The §4.3 honesty mechanism is triggered only by `Waiting`; the scenarios you asked about (`/login`, trust dialog, `/model`, resume picker, usage-limit screen) mostly do not produce a reliable `Waiting`, so the user gets a calm, wrong, *composable* chat while their prompt is eaten by a modal.
- Evidence: plan §4.3:126 "Detection: status `Waiting` + `Notification` hook + WAITING regex bank". Concrete walkthroughs: (a) *first start / trust dialog* — no transcript file exists yet (`cc_conversation_id` is set only on `SessionStart`/`UserPromptSubmit`, wfresult-3 §3.5), so chat shows an empty conversation with a live composer; the `❯` glyph collision is documented to have caused exactly "dispatched prompts vanished into the modal" (wfresult-2 §J, 4791928). (b) *`/login` mid-session* — no hook fires (`Notification` has `auth_success` only on success, report-claude-wrapping.md:318), WAITING bank has no matching token (`status.rs:808-812`: `enter to select|do you want to proceed|❯\s*\d+\.|approve`) → status reads Idle → chat looks healthy while keystrokes land in an OAuth code field. (c) *`/model` or `/resume` typed into the chat composer* — opens an arrow-key picker with no transcript trace at all.
- Fix: invert the trigger. Every chat send opens a **delivery watchdog**: if no matching transcript `user` entry and no `turn_start`/Active transition within N seconds, mark the message *undelivered* (never "sent") and raise the Attention card + peek + Open terminal, regardless of status. Add a second trigger: "transcript file missing/unresolvable while session is running" → Attention card, not empty state. This is the mechanism that makes §4.3 honest for the unknown-unknowns, which is precisely the class it exists for (wfresult-2 lesson 6/22: "Better no feature than one that lies").

[blocker] Fase 0 is the load-bearing gate but has no numeric pass/fail criterion and no designed no-go branch — phases 1-6 are fully specified as if it passes.
- Evidence: plan §2.1:43 "Fase-0 spike must pin down mid-turn write granularity … flush latency"; §7:174 "*Gate: everything downstream cites these fixtures*" — but no threshold, and §8 only mitigates with "hook-driven thinking/tool overlay is the live layer". §1:30 frames message-level arrival as fine because "Grok Bot has no token-level streaming either" — that elides that supermux's *current* renderer streams tokens today, so for a text-heavy turn (an explanation, a plan, a review) chat is a visible latency regression: 3 dots for 30-60s where the terminal showed prose accumulating.
- Fix: write the gate as numbers before the spike runs (e.g. p50 ≤ 2s / p95 ≤ 6s from first assistant token to first visible chat text, measured on a long text-only turn AND a tool-heavy turn), plus the pre-agreed branch if it fails: either accept and design for it explicitly (receipt-first turns, "Claude is writing…" with elapsed timer), or add a partial-text tap (peek-derived tail of the assistant block, clearly marked provisional), or stop the project. A gate with no failing side isn't a gate.

[major] "Context usage / cost … not reliably available from outside the TUI" is factually wrong, and deferring it copies Grok's single most-criticized blind spot into a *coding* tool.
- Evidence: plan §2.3 line 72. Contradicted by report-claude-wrapping.md:516-559 — the statusLine script receives JSON on stdin with `context_window.{limit,used,used_percentage,cached_tokens}`, `cost.total_cost_usd`, `model`, `permission_mode`, `git.branch`, `message_count`, refreshed on message arrival / `/compact` / mode change. supermux already owns the exact install mechanism (`server/src/claude_config.rs:92` `install_hooks`, `supermux-hook` marker, atomic temp+rename) and the payload carries `session_id`, which maps to a supermux session via `cc_conversation_id`. Meanwhile wfresult-4 §12 lists cost blindness as the top user complaint about Grok ("I've used more tokens this month than the last 5 years prior").
- Fix: move a statusLine tap into fase 0/1 (verify: does it clobber a user's existing `statusLine` setting? wrap-or-refuse, and it is a global not per-session setting) and make context% + cost a first-class element of the session header pill. It is the cheapest available differentiator and the one number a power user checks before `/compact`.

[major] The toggle is asserted to be "instant, zero state loss" but the plan never specifies mount/unmount, and the terminal side's re-attach is the single worst-tested operation in this codebase.
- Evidence: plan §4.1:103 "Toggle switch is pure view state → **instant** both ways. The terminal WS machinery … is untouched." But the three call sites (`tile.tsx:213-221`, `desktop-split.tsx:619-629`, `focus/mobile.tsx:~495-515`) are ternaries — a 3-way switch unmounts `LiveTerminal`, so every toggle re-runs the auth→resize→seed handshake whose history is failure classes B and K (wfresult-2 §B: 150ms resize-then-seed handshake, double-delivered overlap bytes, replay scroll; §L: attach-generation re-seed). Unstated: scroll position on return, unread state, composer draft (React textarea vs TUI composer — see finding 2), and what happens if the user toggles *while* a registry keypress is awaiting its dismissal check (§4.3:127), which races the user's own terminal input.
- Also unspecified and load-bearing: **pty geometry when the terminal renderer never runs**. §Global:15 pins geometry to "the terminal renderer / last-known geometry", but after the fase-6 default flip a phone-only user may never mount a terminal — so Claude wraps at whatever the spawn default is, forever, and the Attention card's peek snapshot renders an 80-col dialog into a 390px viewport.
- Fix: (a) keep the terminal *subscribed but detached* (or mounted hidden with geometry frozen — note `d2c333c`: a zero-size fit reflows) and add an explicit toggle-thrash test (100 toggles under a firehose, assert no gap/overlap and no resize storm); (b) define a server-side geometry policy for chat-default sessions (fixed cols/rows at spawn, ≥40 cols); (c) specify draft, scroll-anchor and unread carry-over per direction; (d) scope the `T` hotkey — there is no global hotkey registry in the repo (only `web/src/hooks/use-peek-type.ts:247`), and a bare `T` next to a focused composer is a typing hazard.

[major] `cc_conversation_id` self-heals only on the *next* `SessionStart`/`UserPromptSubmit`, so the chat can render — and accept input against — the wrong (previous) conversation with no visible signal.
- Evidence: wfresult-3 §3.5 / `server/src/hooks.rs:138-147` — captured on those two events only; plan §2.1:38 calls the pointer "already self-healing" and treats it as sound. Windows where it lies: server restart with a live session that hasn't prompted since; a `--resume` pick made in the terminal; `/clear` or a compaction fork before the next prompt; `--fork-session`/`/branch`; a session whose hooks failed to install (the `Json` extractor 415 bug severed the authoritative signal *for weeks*, wfresult-2 §J/b8daf73).
- Fix: add a staleness guard to the tailer — compare the resolved file's `sessionId`/mtime against the session's start time and last hook time; if the pointer predates the current process, render a "reconnecting to this conversation" state (and disable the composer or route through the delivery watchdog) rather than a plausible-looking stale transcript. Probe physically, don't infer (wfresult-2 lesson 11/21).

[major] The modal registry has no version pin, so a Claude Code update silently converts "verified mappings" into wrong keystrokes sent at a confidently-detected dialog.
- Evidence: plan §4.3:127 catalogues mappings in fase 0 and §8 mitigates drift with "every send is verify-after". Verify-after catches *non-dismissal*, not a **wrong dismissal** — if CC reorders options, `2` still dismisses the dialog, having chosen "no, and tell Claude what to do differently" instead of "yes". The repo's own meta-lesson is that a regex claimed live-verified never matched a real frame (`reverify-subagent-live-claims`; wfresult-2 §J meta-lesson).
- Fix: record the Claude Code version that each fixture/fingerprint was captured against (`claude --version` at session spawn, or the version field in the JSONL system entry), and hard-disable registry *actions* on mismatch — degrade to Attention card + Open terminal until re-captured. Add a CI/startup self-test that asserts the fingerprints still match a fresh capture corpus.

[major] The §2.3 state table is missing a large set of interactive states, several of which are core coding-agent workflows rather than edge cases.
- Missing: (1) Claude's own **message queue** (finding 1); (2) **`/rewind` + file checkpoints** — TUI-only, and the most consequential coding feature to hide behind "one toggle away" (report-claude-wrapping.md:449 notes compaction keeps old messages *for* `/rewind`); (3) **`@`-file autocomplete** — the chat composer as specced has no file referencing at all, while the TUI's `@` picker is a primary input mode; supermux already exposes `GET /api/sessions/{name}/tracked-files`; (4) **`/`-command autocomplete** and what `/compact`, `/model`, `/clear`, `/resume`, `/config` typed into the chat composer actually do (some run, some open arrow-key pickers with no transcript trace — see finding 3); (5) **background Bash (Ctrl+B) and `/bashes`** — live output and kill are TUI-only; (6) **MCP elicitation forms** (`Notification` subtypes `elicitation_*`, report-claude-wrapping.md:318) and connector/`requiresUserInteraction` escalations; (7) **ExitPlanMode is not a 2-button card** — plan approval offers auto-accept-edits vs manual-approve vs keep-planning; a yes/no card mismaps the user's intent; (8) **images / non-text content blocks** in user and assistant messages (recall already has a `kind: image`, `recall.rs:104-124`); (9) **hook-blocked tools** (`PreToolUse` deny / exit 2 feedback) rendering as neither success nor failure; (10) **rate-limit + usage-limit screens** and reset times; (11) **permission-mode changes made from the terminal via Shift+Tab** (the chat mode chip must reconcile, `parse_mode` at `status.rs:238` is the only source); (12) **session fork/branch**.
- Fix: extend the §2.3 table with a third column — "chat renders it / Attention card / silently invisible" — and require every row to be one of the first two. Rows that land in "silently invisible" are the ones that will produce wrong states in production. At minimum ship `@`-file and `/`-command pickers in v1 (Grok's `@` popover pattern, wfresult-4 §9, aimed at *files* not agents) — otherwise chat mode is strictly less capable than the terminal for the app's actual job.

[major] Anti-overfitting: the plan inherits Grok's *absences* (no diff/file surface, no cost, prose-only approvals) even though supermux already has the parts and its users are diff-heavy.
- Evidence: wfresult-4 §6 "What is *not* in the UI: no file tree, no diff viewer, no terminal pane, no token/cost meter … no plan/todo checklist widget", and wfresult-4 §12 quotes the HN complaint that every agent UI now looks the same. The plan's only code surface is primitive 7, a diff derived from `Edit`/`Write` tool_use input (§4.2:115) — which yields nothing for `MultiEdit` fan-outs, `NotebookEdit`, or the very common Bash-mediated edits (`sed`, `git apply`, formatters), and never answers "what does my working tree look like now". Unused existing assets: `GET /api/sessions/{name}/git`, `/tracked-files` (wfresult-3 §3.7), `web/src/components/files/code-editor.tsx`, `file-viewer.tsx`, `markdown-viewer.tsx`. Compounding it, §5.2:149 strips the mobile dock to "toggle + mode + interrupt", dropping snippets, attach, dictation and the Ctrl+G external-edit bridge (`desktop-split.tsx:668-676`) from the *default* path — a feature regression, not a simplification.
- Fix: make one coding-native surface part of v1 (a working-tree changes rail off `/git`, with tap-to-open in the existing file viewer, and receipt lines that deep-link to the file they touched) — that plus context/cost is what stops this from being another look-alike. And keep every non-terminal dock action in chat mode; hide only the raw-key joystick/keybar, which is genuinely terminal-specific.

[major] There is a much thinner walking skeleton available, and the reason given for deferring codex/kimi is factually wrong.
- Evidence: `server/src/sessions/recall.rs` (1439 lines) already streams the same JSONL with cursor pagination (`before`), sidechain and system-event flags, sanitisation and caps (`recall.rs:35-38`, :57-74, :140-147) — i.e. most of fase 1's read path exists as a REST endpoint. The plan itself proposes REST-tail for overview tiles (§5.2:150) but a bespoke WS for focus. Meanwhile §7 defers codex/kimi as "no hooks — would ride regex only", yet `server/src/sessions/recall/codex.rs` (535 lines) and `recall/kimi.rs` (783) already parse those transcripts; only the *status overlay* is weaker, not the data plane.
- Fix: reorder to ship a dogfoodable slice in one PR — read-only chat tail rendered from `/recall` + existing SSE status, behind the flag, wired into the *real* focus panel with the toggle (not a `/dev` route). That validates flush latency, fallback fidelity, toggle ergonomics and "does this feel alive" with a real user before the tailer, the WS protocol, the design system or the registry exist. Then upgrade transport (WS tailer) and primitives incrementally. Also cut fase 0's "every TUI dialog variant" sweep down to the dialogs v1 actually *acts on* (permission + plan); the rest only need detection, which the Attention card handles.

[minor] Two smaller but concrete gaps: unbounded entry payloads, and a second motion language.
- Payloads: §2.1 defines `seed`/`entry` frames with no per-entry size cap or backpressure, while `tool_result` routinely carries whole files (a single `Read` of a large file, a 40KB TUI repaint's worth of Bash output). `recall.rs:35-38` already had to cap at `PROMPT_MAX_CHARS=8000` / `REPLY_MAX_CHARS=600`. Fix: per-entry byte cap with a `truncated: true` flag and a fetch-full-by-uuid endpoint, plus a seed byte budget; also state explicitly that transcript content (file bodies, secrets read by Claude) now streams to the browser and to overview tiles, and that the in-memory-only posture of `activity.rs:15-17` does not extend to it.
- Motion: `web/src/lib/springs.ts:1-14` is a framer-motion **spring** bank whose header says "No `transition: all`, no ad-hoc cubic-beziers — PR review enforces this"; §5.1:139 adds two Grok cubic-beziers there and specs CSS `@keyframes`. That is two coexisting motion languages. Fix: either map the Grok curves onto spring equivalents, or extend the existing `tweens` section with an explicit note on why the chat renderer is duration-based — and decide it once, in the plan, not in review.

Sound as written: §6 (toggle persistence via `ui-store` + a new prefs key) matches the existing `overview_layout`/`quick_keys` allowlist pattern at `server/src/prefs.rs:59-65` and needs no migration; the flag + localStorage kill-switch + separate default-flip commit is the correct, proven shape (`TERM_TMUX_HISTORY`).
