# Orchestrator runtime (not part of the buildable repo)
skill/
# Build artifacts
target/
node_modules/
dist/
.vite/
# Playwright e2e run artifacts
test-results/
playwright-report/
# OS
.DS_Store
*.log
.gstack/
# Local deploy config (see .env.example) — ignore every .env variant (incl.
# *.bak / per-host copies) but keep the tracked template.
.env
.env.*
!.env.example
# Deploy artifact written by deploy.sh / deploy-self.sh (records the live SHA)
DEPLOYED_SHA
# Per-session Claude Code state (worktrees, etc.)
.claude/
# Internal-only working dirs (not for OSS)
plan/
agent_notes/
# Local-only notes (private brainstorms / route planning).
ios.md
